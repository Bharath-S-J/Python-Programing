# Define a tuple named 'number'
number = (1, 2, 3)

# Access and print the first element of the tuple (index 0)
print(number[0])

# Count the number of occurrences of the element '2' in the tuple
print(number.count(2))


# Unpacking the tuple into individual variables 'x', 'y', and 'z'
# Define a tuple named 'coordinates'
coordinates = (1, 2, 3)

# Unpack the tuple into individual variables 'x', 'y', and 'z'
x, y, z = coordinates

# Print the value of the variable 'y' (which is the second element of the tuple)
print(y)
